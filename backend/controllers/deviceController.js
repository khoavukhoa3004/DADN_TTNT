const DeviceConstructor = require('../models/device.model').deviceModel;

exports.DeviceController = {
    get: async (req, res) => {
        try {
            
        } catch (error) {
            
        }
    },

    patch: async (req, res) => {

    },
};